# DEPRECATED — legacy Express-only emergency config (required Vite SPA in backend image).
# Phase 3 removed the Vite fallback. Use deploy/scripts/vps-recover-next.sh instead.
#
# If the site is down, on the VPS run:
#   bash deploy/scripts/vps-recover-next.sh
# or:
#   bash deploy/scripts/vps-emergency-restore.sh
